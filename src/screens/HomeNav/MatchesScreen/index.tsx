import React from 'react';
import { View } from 'react-native';

import styles from './styles';

const MatchesScreen = (): JSX.Element => {
  return <View style={styles.container} />;
};

export default MatchesScreen;
